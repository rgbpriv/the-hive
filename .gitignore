Thumbs.db
Desktop.ini
*.log

# Local scratch + server deploy tooling — contains server connection
# details; must NOT be committed to the public repo.
_deploy/
/_*

# Personal resumes / PII — keep out of the public repo
/Ragib.md
/SSA.md
/*.pdf

# Internal docs & markdown notes — keep out of the public repo
/*.md

# Design tokens — kept local for now
/design/
